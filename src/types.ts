export interface IOptions {
  baseUrl: string;
  email: string;
  password: string;
  group: string;
  project: string;
  key?: string;
  cert?: string;
  haBaseUrl?: string;
  haAccessToken?: string;
}
